// import { fromJS } from 'immutable';
// import { makeSelectStoreContainerDomain } from '../selectors';

// const selector = makeSelectStoreContainerDomain();

describe('makeSelectStoreContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
