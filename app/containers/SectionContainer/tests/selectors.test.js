// import { fromJS } from 'immutable';
// import { makeSelectSectionContainerDomain } from '../selectors';

// const selector = makeSelectSectionContainerDomain();

describe('makeSelectSectionContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
