// import { fromJS } from 'immutable';
// import { makeSelectStoreListContainerDomain } from '../selectors';

// const selector = makeSelectStoreListContainerDomain();

describe('makeSelectStoreListContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
